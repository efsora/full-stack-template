/**
 * Input types for Currencies domain
 *
 * These types represent data coming from external sources (HTTP requests, API calls).
 */

// No inputs needed for getSupportedCurrencies - it takes no parameters
