/**
 * Currency Domain Error Types
 *
 * Defines all error types specific to the currencies domain.
 */

/**
 * Union of all currency domain errors.
 */
export type CurrencyError = never; // No specific currency errors yet

/**
 * Note: Currency operations are read-only for now.
 * Future errors might include: CurrencyNotFoundError, CurrencyInactiveError
 */
