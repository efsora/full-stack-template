/**
 * Internal types for Currencies domain
 *
 * These types are used internally within the domain and should NOT be exported
 * from the barrel file (index.ts).
 */

// No internal types needed for currencies at this time
